{
  "name": "edge-cms",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "dev:admin": "turbo dev --filter=admin --filter=sandbox",
    "lint": "turbo lint",
    "format": "turbo format",
    "type-check": "turbo type-check",
    "test:watch": "turbo test:watch",
    "test": "turbo test"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "lefthook": "catalog:",
    "turbo": "2.1.3",
    "typescript": "catalog:"
  },
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=18",
    "yarn": "Use pnpm instead",
    "npm": "Use pnpm instead"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "volta": {
    "node": "22.9.0"
  }
}
